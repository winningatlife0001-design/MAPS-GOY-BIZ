# ── Private client & lead data — NEVER commit (PDPA: personal data) ──
data/leads/*.csv
data/leads/*.json
data/leads/*.xlsx
data/leads/*.md
data/clients/
!data/leads/_template.csv
!data/leads/README.md

# Secrets / local
.env
.env.*
*.key
secrets.*
*.local

# Junk
.DS_Store
Thumbs.db
node_modules/
*.log
.cache/

# Local QA artifacts (Playwright)
qa-out/
/pw*.mjs
